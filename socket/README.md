Socket.io is available as an opt-in extension point.

The current Flutter frontend does not include a realtime client, so sockets are disabled by default.
Set `ENABLE_SOCKET_IO=true` if future booking status updates or chat features need it.
